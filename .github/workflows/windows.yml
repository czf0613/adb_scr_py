name: Windows build and test

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    runs-on: ${{ matrix.os }}
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        include:
          - {os: windows-2025, python: "3.10", arch: x64}
          - {os: windows-2025, python: "3.14", arch: x64}
          - {os: windows-2025, python: "3.14t", arch: x64}
          - {os: windows-11-arm, python: "3.14", arch: arm64}
          - {os: windows-11-arm, python: "3.14t", arch: arm64}
    defaults:
      run:
        shell: pwsh
    env:
      PYTHONUTF8: "1"
      PYTEST_DISABLE_PLUGIN_AUTOLOAD: "1"
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # v10.1.0
        with:
          version: "0.12.13"
          python-version: ${{ matrix.python == '3.14' && '3.14+gil' || matrix.python }}
          enable-cache: true
          cache-dependency-glob: uv.lock
          cache-suffix: windows-${{ matrix.arch }}-${{ matrix.python }}
      - name: Install locked dependencies
        # Optional MCP depends on pywin32, which currently has no cp314t wheel.
        run: uv sync --locked ${{ matrix.python != '3.14t' && '--extra mcp' || '' }} --no-install-project
      - name: Check interpreter architecture
        run: |
          uv run --no-sync python -c "import sysconfig; assert sysconfig.get_platform() == '${{ matrix.arch == 'arm64' && 'win-arm64' || 'win-amd64' }}', sysconfig.get_platform()"
      - name: Build native extension
        run: uv run --no-sync setup.py build_ext --inplace
      - name: Build distribution from source archive
        run: |
          uv build --python .venv/Scripts/python.exe --sdist --out-dir dist
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          $sourceArchive = (Get-ChildItem dist/*.tar.gz).FullName
          uv build --python .venv/Scripts/python.exe --wheel $sourceArchive --out-dir dist
      - name: Install built wheel
        run: |
          $wheel = (Get-ChildItem dist/*.whl).FullName
          uv pip install --python .venv/Scripts/python.exe --no-deps $wheel
      - name: Validate installed package and system media without a phone
        run: uv run --no-sync python -Werror::RuntimeWarning .github/scripts/check_windows.py "${{ matrix.python }}" ${{ matrix.python == '3.14t' && '--core-only' || '' }}
      - uses: actions/upload-artifact@v7
        if: ${{ !cancelled() }}
        with:
          name: windows-${{ matrix.arch }}-${{ matrix.python }}
          path: |
            dist/*.tar.gz
            dist/*.whl
            test-results.xml
          retention-days: 7
